server {
    listen 80;
    location / {
        include proxy_params;
        # proxy_pass http://unix:/tmp/neocrisis.sock;
        proxy_pass http://unix:{{ proj_folder }}/api/neocrisis.sock;
    }
}

server {
    server_name www.neocrisis.xyz;
    return 301 https://github.com/dutc/neocrisis;
}
